# Skill Name: Logistics_Spatial_Logic
# Category: Geometry, Pathfinding, Grid-Constraint

## Core Objective
確保所有物流輸送帶 (Transport Line) 的修改、延伸或路由計算，均符合網格對齊、佔用規則，且禁止出現無效的空間拓撲。

## Operational Rules
1. **SSOT (Single Source of Truth) Constraint:**
   - 任何涉及「佔用判定」的邏輯，必須優先調用 `ConveyorRouter.validateRouteFootprint` 或 `getGhostOccupiedCells`，嚴禁在外部自行撰寫複雜的座標檢查。
   - 修正路徑時，必須先更新數據結構（Path/JSON），最後才觸發物理渲染。

2. **Directional Topology Rules:**
   - **Extension Logic:** 延伸物流線時，必須優先讀取 `sourceLine.turnArrowOverride`。禁止為了縮短距離而強制斷開原有的 `turnArrowOverride` 邏輯。
   - **Corner Handling:** 所有路徑點若產生轉折 (`isCorner`)，必須確保該點的 `dirIn` 與 `dirOut` 符合網格正交規則（僅 0/90/180/270 度），禁止出現對角線 (Diagonal) 連接。

3. **Collision Avoidance:**
   - 修改邏輯時，必須包含 `onCollision` 回調函數。
   - 在計算 `isValidNode` 時，若 `ghost` 與既有的 `logisticsOccupiedKeys` 衝突，則該修改無效。

4. **Validation Checklist (Before Commit):**
   - [ ] 是否檢查過路徑寬度 (`routeWidth`) 對佔用格的偏移影響？
   - [ ] 是否在延伸線段時檢查過端點 (`endpoint`) 的 `splitSequenceOrder`？
   - [ ] 對於 `isMerger` 邏輯，是否確保沒有觸發逆向物流衝突 (180度 U-Turn)？

## Error Prevention Protocols
- **U-Turn Prevention:** 當計算兩個 `Grid Point` 的向量差時，若 `Math.sign(dx) === -Math.sign(startVec.x)`，必須立即回退 (Fallback) 到 `AStar` 模式，禁止使用 `L-Shape`。
- **Snap Point Stability:** 在處理 `getSnapPoint` 時，若距離小於 `tileSize * 1.5`，必須強制吸附至 `ent.x/y`，不得在吸附點之間留下孤立格。