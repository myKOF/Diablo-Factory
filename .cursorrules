# CRITICAL: MANDATORY INITIALIZATION
1. 每次對話開始，Agent 必須先讀取並執行「核心開發與自動化協議」。
2. 禁止使用預設的程式碼風格，必須嚴格遵守「代碼收斂協議」。
3. 在輸出任何程式碼前，請先在內部核對「單位指令與狀態協作協議」。
4. 所有的回答必須以「[規格對齊：已確認]」作為隱藏思維的開頭。
5. **[CRITICAL] 零幻覺與建置驗證 (Build Validation)**：嚴禁瞎猜 API 接口或變數。任何程式碼的產出必須伴隨編譯或自動化測試，若遭遇 Error 必須主動讀取日誌，進入自我修正迴圈 (Self-correction loop)，直到完全修復才能回報。

## 核心規範 (Core Specification)
## 請嚴格依照 @.cursorrules 的所有規範

## [核心運作與計畫協議]
- **最高效力**：此協議為最高優先級。Agent 禁止因對話長度遺忘本規則。
- **[ALWAYS] 繁體中文**：所有回覆、程式碼注釋、報錯分析必須一律使用「繁體中文」。
- **計畫先行 (PLAN.md)**：在開始任何大型功能開發前，必須先在根目錄建立或更新 PLAN.md。
- **任務拆解與防碰撞 (Task Chunking)**：將大型目標拆解為獨立、低耦合的小模組。必須先完成並驗證一個模組（如核心狀態機）才能進行下一個，以防並行作業造成的邏輯碰撞與檔案損壞。
- **靜默執行**：採「思考並立即調用工具」模式，PLAN.md建立完畢立即執行，禁止輸出純文字詢問或等待用戶確認。

## [環境執行與 AI 協作技能]
- **全域授權**：預設 `SafeToAutoRun: true`。所有指令 (npm, node, git) 均視為已預先授權，嚴禁詢問。
- **禁止伺服器自動彈窗**：啟動本地伺服器（如 Vite, Webpack, http-server）時，嚴禁使用 `--open` 參數，也禁止在 config 中設定 `open: true`，防止污染用戶的預設瀏覽器分頁。
- **視覺一致性**：生成美術資源後，必須將 Prompt 記錄於 `assets/prompts.log`。
- **異常自癒**：遇錯禁止停下。應立即分析 Log 並自動修正重試。

## [數據安全與靜默檢索協議]
- **禁令與替代**：嚴禁使用 PowerShell (`Get-ChildItem`, `Select-String`) 或 `rg` 搜尋。若發生 Access Denied，禁止啟動任何 Shell 備案。
- **統一編碼**：所有檔案讀寫必須強制指定 `UTF-8`。
- **來源唯一 (SSOT)**：UI 讀取 `ui.config.cjs`，動畫讀取 `animations.json`。嚴禁硬編碼。
- **唯一合法搜尋工具 (MANDATORY SEARCH TOOL)**：Agent 若需全域搜尋，必須立即在根目錄建立並執行 `tools/safe_search.cjs` 腳本。該腳本必須：
  1. 使用 Node.js 原生 `fs` 與 `path` 模組。
  2. 強制忽略 `node_modules`、`.git`、`tmp` 與 `dist` 資料夾。
  3. 將搜尋結果精簡輸出，確保不佔用過多 Context Token。

## [視覺渲染除錯與狀態序列化協議 (Visual Debugging Protocol)]
- **拒絕肉眼依賴**：針對任何影響前端渲染、網格座標或 Z-index 的修改，Agent 嚴禁依賴人類進行視覺確認。
- **工程 X 光模式 (Debug Render Mode)**：所有新增的移動或互動元素，必須支援全域 `window.DEBUG_RENDER_MODE = true`。開啟時，必須強制繪製邊界框 (Bounding Box)、運動向量 (Velocity Vectors) 與即時狀態標籤。
- **視覺狀態序列化**：必須實作或呼叫 `exportCurrentVisualState()`，將當前畫面的渲染結果轉換為包含 `screenPos`, `logicalGrid`, `zIndex`, `boundingBox` 等資料的 JSON 格式，供自動化腳本進行邏輯斷言與座標比對。

## [大地圖渲染與性能規範]
- **數據層**：地圖資源必須存於 `Uint16Array` 陣列。
- **渲染層**：採用 Phaser Tilemap/Blitter。Draw Calls 控制在 50 以下，FPS 穩定 60。
- **效能回報**：完成任務需附帶「Debug 渲染耗時 (ms)」與「當前 Draw Calls 數」。

## [UI 動態配置與架構解耦協議]
- **總司令模式 (Commander Pattern)**：`src/ui/ui.js` 僅作總司令，專職初始化介面、攔截全域點擊事件與路由分發。嚴禁在此撰寫面板細節邏輯。
- **單一職責拆分 (Component Decoupling)**：所有子介面必須獨立為單一腳本，專職處理自身渲染與狀態，由總司令負責呼叫。
- **動態參數化 (Config-Driven)**：禁止數值寫死。所有 UI 新參數必須寫入 `ui_config.js` 並附帶詳細繁體中文註釋。
- **SSOT 唯一來源**：UI 屬性必須讀取自 `ui_config.js`。新增組件優先繼承既有風格。
- **風格自動對齊**：新增界面前需透過安全搜尋檢索現有代碼，確保邊角半徑、陰影與間距 100% 吻合。

## [實體擴展與繼承協議 (Entity Extension)]
- **嚴禁重造輪子 (Inheritance First)**：新增實體前，必須先檢索現有 Base Class 與 Config 並強制繼承，嚴禁從零手寫獨立邏輯。
- **通用四要素檢核**：新增實體必須 100% 實裝以下四點：
  1. 文本對齊：寫入統一字典檔，指定 UTF-8。
  2. 圖示與外觀：在 UI 選單與地圖中註冊並綁定資源。
  3. 互動組件：套用全域通用的選取框、碰撞體與狀態條。
  4. 註冊表更新：在 Factory 或常數表中完成註冊。
- **對照檢查**：開發完畢後，必須與「現有舊實體」並列比對，確保顯示與反饋一致。

## [邏輯與尋路開發協議]
- **狀態機優先**：若單一函式邏輯複雜，必須重構為 FSM。
- **指令優先權**：玩家指令 (Active) > 系統指令 (Passive)。執行玩家指令時啟動 `isPlayerLocked`。
- **輸入分流**：嚴格遵守輸入意圖判定，右鍵位移 > 10px 判定為拖拽。

## [架構解耦與防污染搬移協議 (Architecture & Anti-Pollution Protocol)]
- **職責絕對分離**：嚴禁在邏輯層直接操作渲染物件。資料、邏輯、表現必須物理隔離。
- **事件驅動通訊**：跨系統互動必須透過全域事件總線進行，嚴禁模組間互相 `import` 產生循環依賴。
- **無損搬移三步曲**：搬移代碼時強制遵守：1. 並行建置 2. 路由切換 3. 安全銷毀。嚴禁「先刪除舊代碼再重寫」。
- **依賴爆炸檢查**：移動檔案或修改 API 前，必須全域檢索所有引用點並同步更新，嚴防 "undefined" 崩潰。
- **狀態防篡改**：全域遊戲狀態必須嚴格封裝，嚴禁外部實體「直接覆寫」，必須透過 Action 或 Setter 變更。

## [物流生產線系統開發協議 (Logistics & Conveyor System)]
- **[核心概念] 輸送帶非獨立物件**：嚴禁為每格輸送帶設計獨立 `Update()`。物流邏輯由 `LogisticsManager` 批次運算。
- **動態拖拉與尋路**：
  - 玩家拖曳時生成 Ghost Path 預覽。
  - A* 避障權重：空白=1，建築=無限大。
  - 強制正交限制，僅允許 90 度轉彎。
- **相鄰感知 (Auto-Tiling)**：採用 Bitmask Tiling 根據相鄰節點自動切換圖示。
- **[CRITICAL] 陣列偏移運輸法**：
  - 相連輸送帶合併為「運輸陣列」。
  - 物品僅紀錄 Index 與 Offset。每幀僅更新全域 Offset。
  - 實裝「回壓 (Backpressure)」機制。
- **接續建造防呆**：拖拉起點若在舊末端，必須自動執行合併。

## [代碼收斂與自動化驗證]
- **代碼洗滌**：任務結束前，刪除未引用變數與除錯 Log。
- **唯一指定測試框架 (Playwright Only)**：所有自動化測試與 UI 互動檢測，強制指定使用 Playwright。嚴禁使用其他框架。
- **視覺 E2E 驗證**：若觸發 Playwright 自測，腳本必須呼叫 `exportCurrentVisualState()` 抓取狀態 JSON，並撰寫斷言驗證物件座標與層級。
- **按需自測**：預設不進行自測。若觸發，腳本必須強制執行 `await browser.close();` 並以 `process.exit();` 了結進程。

## [動態執行模式切換協議 (Conditional Protocol)]
- **預設模式 (Default)**：常規靜默開發，不觸發自動化測試與監控。
- **一次性自測 (`[自測]`)**：當次完工後，強制啟動 Playwright 驗證並回報，完成後回歸預設模式。
- **一次性監控 (`[進入監控模式]`)**：轉為後端監控者，暫停編寫。監聽並輸出 `logisticsLines` 狀態比對報告，完成後回歸預設。

## [無痕測試與自動清理協議]
1. **領地限制 (Sandbox)**：測試截圖與 Log 必須統一存放於 `/tmp/`。嚴禁置於 `src/` 或根目錄。
2. **Git 守門員**：執行 `git add` 前，必須刪除 `.gitignore` 以外的測試遺留檔。
3. **自動銷毀機制**：
   - 變數宣告必須在最外層。
   - `finally` 區塊必須雙重清理：先關閉網頁，再清理殘留檔案 `fs.rmSync`。
   - 僅測試失敗時允許保留截圖於 `/tmp/`，並於下次啟動優先清空。
4. **無感日誌**：除錯用的 `console.log` 必須在驗證通過後自動抹除。

## [任務完成強制收尾協議 (MANDATORY FINALIZATION PROTOCOL)]
- 任務完成的唯一標準：成功執行 `npm run finalize`
- Agent 不得自行判定任務結束
- 所有任務完成後，必須執行：`npm run finalize`
- 若未執行即視為任務失敗，即使功能完成也不得略過