核心規範 (Core Specification) - Antigravity 進階優化版
[核心運作與計畫協議]
最高效力：此協議為最高優先級。Agent 禁止因對話長度遺忘本規則。
[ALWAYS] 繁體中文：所有回覆、程式碼注釋、報錯分析必須一律使用「繁體中文」。
計畫先行 (PLAN.md)：在開始任何大型功能開發前，必須先在根目錄建立或更新 PLAN.md，定義玩家目標、核心迴圈 (Main Loop)、輸入控制、勝敗條件與開發里程碑。
靜默執行：採「思考並立即調用工具」模式，禁止輸出純文字詢問或等待用戶確認。

[環境執行與 AI 協作技能]
全域授權：預設 SafeToAutoRun: true。所有 powershell, npm, node 指令均視為已預先授權，嚴禁詢問。
視覺一致性 (Asset Spec)：若需生成美術資源，必須將 Imagegen 的 Prompt 記錄於 assets/prompts.log，確保風格統一。
技能調用：優先調用 $playwright-interactive 進行即時瀏覽器測試，並利用 $openai-docs 檢索最新的 API 規範。
異常自癒：遇錯禁止停下。應立即分析 Log、更換路徑或修正環境並自動重試。

[數據安全與靜默檢索協議]
禁令與替代：嚴禁使用 PowerShell 搜尋。必須使用 Node.js (fs) 腳本遍歷檔案，以避開系統攔截。
統一編碼：所有檔案讀寫必須強制指定 UTF-8 (with BOM)。若出現亂碼，必須立即切換編碼重讀。
來源唯一 (SSOT)：所有 UI 讀取 ui.config.cjs，動畫讀取 animations.json。嚴禁硬編碼。

[大地圖渲染與性能規範]
數據層：全地圖資源必須存於 Uint16Array 陣列。
渲染層：採用 Phaser Tilemap/Blitter，僅對視口 (Viewport) 數據動態實例化。Draw Calls 必須控制在 50 以下，FPS 穩定 60。
效能指標：任務完成需附帶「Debug 渲染耗時 (ms)」與「當前 Draw Calls 數」。

[邏輯與尋路開發協議]
狀態機優先：若單一函式修改超過 5 次，必須重構為 FSM。單位動作中斷與鎖定機制為強制規範。
全能尋路 (EasyStar)：開啟對角線尋路。若目標為障礙物，自動執行 getNearestFreeTile()。
指令優先權：玩家指令 (Active) > 系統指令 (Passive)。執行玩家指令時啟動 isPlayerLocked，屏蔽自動化行為。

[代碼收斂與自動化驗證]
代碼洗滌：任務結束前，強制刪除所有未引用的變數、函式與除錯用 console.log。
自動自測 (Self-Testing)：檔案存檔後必須自動啟動 Headless 瀏覽器 (Playwright) 進行功能驗證。
憑證回報：回覆時必須附帶「驗證結果摘要」與「截圖路徑」，並將工作紀錄更新至 .logs/ 資料夾中。
進程自毀：測試結束必須執行 finally { browser.close(); process.exit(); }，保持環境純淨。