# 核心規範 (Core Specification)

[核心運作協議 (Core Protocols)]
此專案遵循以下核心規範，套用於所有對應今日對話與未來所有對話與操作中：


[核心授權與執行規範協議]
1. 全局授權：預設 SafeToAutoRun: true。所有 powershell, npm, node 指令均視為已預先授權，嚴禁彈窗詢問。
2. 靜默策略：優先使用 Node.js 內部 API (fs) 處理檔案讀寫與搜尋，以避開 Shell 攔截；執行指令時自動補完 -y 或 --yes 參數。
3. 執行流程：採「自動擬定計劃 (Auto-Draft)」模式，擬定後立即執行，無需等待用戶確認。
4. 異常自癒：若執行受阻或報錯，禁止停下等待。應立即分析 Error Log、更換 API 路徑或修正環境並自動重試。
5. 進程清理：所有測試或臨時指令執行完畢後，必須自動 Kill/Close 相關進程，保持環境純淨。
6. 最高效力：此協議為最高優先級，適用於所有情況與新對話，禁止因對話長度遺忘。

[性能與穩定性協議]
1. 效能優化：實作空間分區(Grid-based)與批次渲染。UI 採髒值檢查(Dirty Flag)機制，禁止無謂重繪。
2. 物件池化：循環內避免 new 物件，減少垃圾回收(GC)負擔。
3. 邏輯保險：資源扣除必須有非負檢查。所有 FSM 任務需具備 Try-Catch 隔離，防止單點錯誤導致循環中斷。
4. 穩定優先：優化時嚴禁改動核心任務隊列(TaskQueue)的 push/pop 順序，確保建築/採集邏輯鏈條完整。

[UI設計規範協議]
1. **UI設計規範**：(Responsive UI Skill)所有界面在製作時，必需同時寫入ui.config.cjs讓其尺寸、顏色、字體等參數自訂義，且需附帶詳細中文注釋。詳細規範如下：
2. 介面參數解耦 (UI Config Injection)規範：禁止在邏輯代碼中硬編碼（Hard-coded）任何尺寸、顏色或字體。
3. 來源：所有表現層參數必須讀取 ui.config.cjs。
4. 注釋：該文件中的每個變數必須附帶詳細的 中文功能注釋。
5. 錨點對齊規範 (Anchor-Based Positioning)禁止絕對座標：嚴禁使用固定座標（如 x: 100, y: 100）定位 UI。
6. 邊界對齊：所有 UI 元素必須根據畫布（Canvas）邊界計算位置：資源列：左上對齊 (Top-Left) + 間距。動作日誌：右下對齊 (Bottom-Right) + 間距。建造選單：中下對齊 (Bottom-Center)。Resizing：視窗尺寸改變時，必須自動重新計算座標（Recalculate on Resize）。
7. 動態等比縮放 (Dynamic Scaling)：基準解析度以 1920x1080 為設計基準。
8. 比例因子 (Scale Factor)：計算當前寬度與基準寬度的比例 $Scale = CurrentWidth / 1920$。
9. 縮放應用：所有字體大小、圖標尺寸、間距必須乘以 $Scale$，確保在不同解析度下比例一致。
10. 色彩格式規範：所有顏色表示格式強制統一使用 6 位 HEX 格式（如 #fbc02d）。

[自我驗證協議]
1. 檔案更新後，必須執行 Node.js 腳本呼叫 Playwright/Puppeteer 開啟在地網頁。網頁開啟不可彈出只需在背景執行，以免影響用戶其它工作。
2. 驗證條件：Canvas 需渲染、無 Console Error、全域邏輯變數符合預期。
3. 流程控制：驗證成功後立即關閉瀏覽器與 Server 進程。
4. 失敗處理：若測試未過，需自動截圖至 temp/ 並嘗試修正代碼，直到通過為止。
5. 嚴禁詢問：整個「測試-關閉-修正」過程需符合 SafeToAutoRun，不需人工介入。

[CSV 數據安全規範協議]
1. 權限：預設對 CSV 採「唯讀」模式。非經用戶顯式指令與特定授權，嚴禁執行任何寫入或修改。
2. 編碼：修改或產出 CSV 必須強制使用 UTF-8 (含 BOM) 編碼，確保繁體中文無亂碼。
3. 讀取：透過終端機(PowerShell)讀取時，必須顯式帶上參數 -Encoding UTF8。
4. 結構：修改授權僅限於數值變更，嚴禁更動表單標頭(Headers)與隱藏註釋。

[檔案編碼規範協議]
1. 終端機讀取：使用 PowerShell 讀取時必須強制帶上參數 `-Encoding UTF8`。
2. 檔案存取：所有 fs.readFile/Write 必須指定 'utf8'。
3. 嚴禁亂碼：若讀取結果出現  符號，Agent 必須立即嘗試更換編碼重新讀取，不得直接輸出亂碼給用戶。

[暫存檔管理規範協議] (Temporary Files Management)
- **自動清理機制**：所有由 Agent 或自動化腳本產生的暫存檔（如 Debug 截圖、Log、臨時生成的 node_modules）必須在任務執行成功（Success）後立即刪除。
- **失敗保留機制**：僅在測試失敗（Failed）時，允許保留最後一組截圖於 `tmp/` 目錄中，並將其路徑回傳給開發者供 Debug 使用。
- **儲存位置限制**：嚴禁在 `src/` 或根目錄下產生任何臨時檔案，所有暫存行為必須統一導向至 `tmp/`。
- **Node.js 執行協議**：所有 Node.js 腳本在調用 Playwright/Puppeteer 後，必須在 `finally` 區塊中執行 `fs.rmSync` 清理 `tmp/`。

[靜默檢索協定]
1. 禁令：嚴禁使用 PowerShell (Get-ChildItem/Select-String) 進行檔案搜尋或內容檢索。
2. 代替：必須改寫 .cjs 腳本利用 fs 模組執行遍歷與過濾。
3. 泛用：適用於所有代碼搜尋及 CSV 欄位讀取。
4. 執行：利用 node 直接執行腳本並獲取結果，確保完全自動化且不觸發系統彈窗。
5. 編碼：腳本讀寫必須強制使用 UTF-8。

[其它備註]

「以後不要再用 PowerShell 搜尋字串了，這會一直觸發系統詢問。請改寫一個簡單的 .cjs 腳本來遍歷檔案並搜尋 model_size，然後直接執行該腳本。」