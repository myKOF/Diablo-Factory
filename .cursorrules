# 核心規範 (Core Specification)

[核心運作協議 (Core Protocols)]
此專案遵循以下核心規範，套用於所有對應今日對話與未來所有對話與操作中：

[指令自動授權協議]
1. 靜默執行：所有 Run Command (npm, npx, node等) 均視為已預先授權。
2. 禁止詢問：嚴禁跳出「是否允許執行」視窗；預設 SafeToAutoRun: true。
3. 自動補完：執行指令時自動加上 -y 或 --yes 參數確保流程不中斷。
4. 錯誤自癒：指令失敗時禁止停下，應立即根據 Error Log 自行修正環境並重新執行。
5. 環境清理：測試指令執行完畢後，必須自動 Kill/Close 相關進程。
6. 自動執行計劃 (Auto-Draft plan)：在執行前先擬定計劃，但無需等待確認立刻執行。
7. 表單修改禁止 (CSV Modification is prohibited)：除非用戶特別指示並授權，否則嚴禁直接修改用戶的csv表單。若用戶授權創建或修改csv表單，優先確保不能產生繁體中文亂碼問題。

[性能與穩定性協議]
1. 效能優化：實作空間分區(Grid-based)與批次渲染。UI 採髒值檢查(Dirty Flag)機制，禁止無謂重繪。
2. 物件池化：循環內避免 new 物件，減少垃圾回收(GC)負擔。
3. 邏輯保險：資源扣除必須有非負檢查。所有 FSM 任務需具備 Try-Catch 隔離，防止單點錯誤導致循環中斷。
4. 穩定優先：優化時嚴禁改動核心任務隊列(TaskQueue)的 push/pop 順序，確保建築/採集邏輯鏈條完整。

[UI設計規範協議]
1. **UI設計規範**：(Responsive UI Skill)所有界面在製作時，必需同時寫入ui.config.cjs讓其尺寸、顏色、字體等參數自訂義，且需附帶詳細中文注釋。詳細規範如下：
2. 介面參數解耦 (UI Config Injection)規範：禁止在邏輯代碼中硬編碼（Hard-coded）任何尺寸、顏色或字體。
3. 來源：所有表現層參數必須讀取 ui.config.cjs。
4. 注釋：該文件中的每個變數必須附帶詳細的 中文功能注釋。
5. 錨點對齊規範 (Anchor-Based Positioning)禁止絕對座標：嚴禁使用固定座標（如 x: 100, y: 100）定位 UI。
6. 邊界對齊：所有 UI 元素必須根據畫布（Canvas）邊界計算位置：資源列：左上對齊 (Top-Left) + 間距。動作日誌：右下對齊 (Bottom-Right) + 間距。建造選單：中下對齊 (Bottom-Center)。Resizing：視窗尺寸改變時，必須自動重新計算座標（Recalculate on Resize）。
7. 動態等比縮放 (Dynamic Scaling)：基準解析度以 1920x1080 為設計基準。
8. 比例因子 (Scale Factor)：計算當前寬度與基準寬度的比例 $Scale = CurrentWidth / 1920$。
9. 縮放應用：所有字體大小、圖標尺寸、間距必須乘以 $Scale$，確保在不同解析度下比例一致。

[自我驗證協議]
1. 檔案更新後，必須執行 Node.js 腳本呼叫 Playwright/Puppeteer 開啟在地網頁。網頁開啟不可彈出只需在背景執行，以免影響用戶其它工作。
2. 驗證條件：Canvas 需渲染、無 Console Error、全域邏輯變數符合預期。
3. 流程控制：驗證成功後立即關閉瀏覽器與 Server 進程。
4. 失敗處理：若測試未過，需自動截圖至 temp/ 並嘗試修正代碼，直到通過為止。
5. 嚴禁詢問：整個「測試-關閉-修正」過程需符合 SafeToAutoRun，不需人工介入。
